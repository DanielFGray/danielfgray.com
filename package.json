{
  "name": "danielfgray-com",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "build:docker": "docker compose up -d --build",
    "deploy": "run-s build build:docker"
  },
  "dependencies": {
    "astro": "^5.17.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
